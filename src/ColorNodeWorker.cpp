//
// Created by rolf on 25-2-23.
//

#include "pcog/ColorNodeWorker.hpp"
namespace pcog{
void ColorNodeWorker::processNode(BBNode &node) {
   //Node preprocessing to compute focus graph and complete focus graph
   //Set up LP representation (use rows limited to focus graph and fix irrelevant columns)
   //Price-lp loop
   //Branch: Check if node is cut off. If not, determine the branching vertex pair of the graph.
}
}

